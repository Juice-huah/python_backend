# FastAPI Payment Simulator API

This project is a simulator that handles generate QR, view callbacks, approved, and decline payment using FastAPI.

## Prerequisites
Before running this project, ensure you have **Python 3.10+** installed.

## Installation
To run this project, you need to install the following Python libraries:

```bash
pip install fastapi[standard] pydantic

